{
  "name": "server",
  "version": "1.0.0",
  "description": "this is the backend of simple crud operation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server -p 8000 -w db.json"
  },
  "author": "Balaram Gayen",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
